import { atom, useAtom } from 'jotai'

export const notifications = atom([]);
export const FEATURE__LOCALIZATION_MANAGER____localizationManagerList = atom([]);
export const FEATURE__LOCALIZATION_MANAGER____editMessageIndex = atom(null);