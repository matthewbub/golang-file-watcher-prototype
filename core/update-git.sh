#!/bin/bash

# Change to the Git repository directory
cd ~/../../Volumes/git/ninembs.studio

# Checkout the 'main' branch
git checkout main

# Pull the latest changes from the remote repository
git pull
