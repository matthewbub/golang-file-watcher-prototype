version: '3'

services:
  ieportals:
    build:
      context: .
      dockerfile: Dockerfile
    image: ieportals-image
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development